## What

<!-- What does this change and why? Link the related issue if there is one. -->

## Checklist

- [ ] `zig build test` and `zig build fmt` pass (or `pnpm lint && pnpm typecheck && pnpm test` for `apps/web`)
- [ ] Tests added or updated for behavior changes
- [ ] No new subsystem or dependency without a prior issue (minimalism is a project constraint — see [CONTRIBUTING.md](../CONTRIBUTING.md))
